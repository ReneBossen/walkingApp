{
  "name": "stepper-scripts",
  "version": "1.0.0",
  "description": "Scripts and utilities for the WalkingApp/Stepper project",
  "scripts": {
    "seed": "npx ts-node scripts/seed-test-data.ts",
    "seed:cleanup": "npx ts-node scripts/seed-test-data.ts --cleanup"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.90.1",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "supabase": "^2.72.8",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
